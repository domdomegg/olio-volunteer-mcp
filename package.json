{
  "name": "olio-volunteer-mcp",
  "version": "0.0.0",
  "description": "MCP server for the Olio Volunteer Hub - browse Food Waste Hero collections, your squads, and your collection history.",
  "license": "MIT",
  "author": "Adam Jones (domdomegg)",
  "repository": {
    "type": "git",
    "url": "https://github.com/domdomegg/olio-volunteer-mcp.git"
  },
  "bin": "dist/main.js",
  "files": [
    "dist"
  ],
  "mcpName": "io.github.domdomegg/olio-volunteer-mcp",
  "scripts": {
    "start": "npm run build && node ./dist/main.js",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "lint": "eslint",
    "clean": "rm -rf dist",
    "build": "tsc --project tsconfig.build.json",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "devDependencies": {
    "@tsconfig/node-lts": "^22.0.2",
    "@types/node": "^22.0.0",
    "eslint": "^9.32.0",
    "eslint-config-domdomegg": "^2.0.9",
    "tsconfig-domdomegg": "^1.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "~1.24.0",
    "zod": "^3.24.0"
  }
}
