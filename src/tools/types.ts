export type Config = {
	/** Value of the `_session_id` cookie from a logged-in Volunteer Hub session. */
	sessionId: string;
	/** Base URL of the Volunteer Hub (default: https://volunteers.olioex.com). */
	baseUrl: string;
};
